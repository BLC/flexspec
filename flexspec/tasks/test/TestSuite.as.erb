package {
	import com.blchq.unit.FilteringTestSuite;

<% @packages.each do |package| %>
	import <%= package %>.*;
<% end %>

	/*
		This file is autogenerated by rake <%= @task_name %>. Manual changes to it
		are going to be blown away next time it is generated, so don't edit it if you
		want to keep your changes around.
	*/
	public class <%= @test_suite_name %> extends FilteringTestSuite {
		public function <%= @test_suite_name %>(param:Object=null) {
			super(param);
			<% if @test_initializer %>new <%= @test_initializer %>();<% end %>
			
			addTests();
		}

		protected function addTests():void {
<% @packages.each do |package_name| %>
			// <%= package_name %>
			
<% @package_test_sets[package_name].each do |test| %>
			addTestSuite(<%= test %>)
<% end %>

<% end %>
		}
	}
}